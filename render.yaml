services:
  - type: web
    name: roveri-backend
    env: python
    rootDir: Projeto/RoveriBack_final
    buildCommand: pip install -r requirements.txt
    startCommand: daphne -b 0.0.0.0 -p $PORT project.asgi:application
    autoDeploy: true
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
      - key: DJANGO_SETTINGS_MODULE
        value: project.settings
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: DATABASE_URL
        fromService:
          name: roveri-db
          type: pserv
          property: connectionString
      - key: CORS_ALLOWED_ORIGINS
        value: "https://roveri-frontend.onrender.com"
      - key: CSRF_TRUSTED_ORIGINS
        value: "https://roveri-frontend.onrender.com"

  - type: web
    name: roveri-frontend
    env: static
    rootDir: Projeto/Roveri_final
    buildCommand: npm ci && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        value: "https://roveri-backend.onrender.com/api"

databases:
  - name: roveri-db
    plan: free
    databaseName: roveridb
    user: roveriuser
